# vmsecd allowlist dir for the rig. Empty for now — the secrets-managers
# register into the fleet and run the KDBX daemon, but no client paths are
# allowlisted yet (allowlist content is part of the deferred demo-data work).
# Add <name>.json files here (see waf/secrets-manager/allowlist.d.example).
