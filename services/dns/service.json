{
  "version": "0.0.6",
  "service": {
    "name": "dns",
    "description": "Rig resolver stack (per dns VM): redis + coredns + dns-bridge.",
    "sources": {
      "coredns":    { "image": "coredns-redis", "version": "0.2.1-9c45d13" },
      "dns-bridge": { "image": "dns-bridge",    "version": "0.2.1-9c45d13" }
    },
    "docker": { "projectName": "dns", "volumes": [ { "dns-seed": "keep" } ] },
    "_comment_assets": "dns-seed = the bootstrap zone artifact (prod publishes zone-<zone>-seed via terraform oci_artifacts). compose-up (dev) skips asset staging by design — vm-stack fills the volume from seed/rig.internal.zone; Step 2's attested run pulls the real artifact and pins its version here.",
    "assets": { "dns-seed": { "artifact": "zone-rig-internal-seed", "version": "0.0.1" } },
    "templates": [
      { "source": "templates/Corefile", "destination": "config/Corefile", "overwrite": true }
    ],
    "files": [ "service.json", "compose.yml", "templates/Corefile" ],
    "variables": { "IMAGE_REDIS": "redis:7-alpine" }
  }
}
